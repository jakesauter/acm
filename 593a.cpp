#include <iostream>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

int main()
{
	int lines;
	 chars;
	cin >> lines;
	vector<string> v(lines);
	for(int i=0;i<lines;i++)
	{	
		getline(cin, v[i]);
		for(int i=0;i<v[i].length;i++)	
		{
			
		}
	}
	return 0;
}
